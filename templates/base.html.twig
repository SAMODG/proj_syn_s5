<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{% block title %}Gestion Boutique{% endblock %}</title>

    <!-- Bootstrap CSS -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body>
    <div class="container-fluid">
        <div class="row">
            <!-- Barre latérale -->
            <nav class="col-md-2 bg-primary text-white min-vh-100 py-3">
                <h3 class="text-center">Gestion Boutique</h3>
                <ul class="nav flex-column">
                    <li class="nav-item">
                        <a class="nav-link text-white" href="{{ path('app_boutiquier') }}">Dashboard</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link text-white" href="{{ path('boutiquier_clients') }}">Clients</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link text-white" href="#">Paiement de Dettes</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link text-white" href="#">Articles</a>
                    </li>
                    <!-- Ajout des liens vers vos deux autres contrôleurs -->
                    <li class="nav-item">
                        <a class="nav-link text-white" href="{{ path('admin_dashboard') }}">Admin Dashboard</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link text-white" href="{{ path('client_dashboard') }}">Espace Client</a>
                    </li>
                </ul>
            </nav>

            <!-- Contenu principal -->
            <main class="col-md-10">
                {% block content %}{% endblock %}
            </main>
        </div>
    </div>

    <!-- Bootstrap JS -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
