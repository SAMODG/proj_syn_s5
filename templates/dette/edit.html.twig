{% extends 'base.html.twig' %}

{% block title %}Modifier une Dette{% endblock %}

{% block body %}
<h1>Modifier la Dette</h1>

{{ form_start(form) }}
    {{ form_widget(form) }}
    <button class="btn btn-primary">Enregistrer</button>
{{ form_end(form) }}

<a href="{{ path('dette_index') }}" class="btn btn-secondary">Retour à la liste</a>
{% endblock %}
